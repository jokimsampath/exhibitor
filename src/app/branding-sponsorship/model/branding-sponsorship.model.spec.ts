import { BrandingSponsorshipModel } from './branding-sponsorship.model';

describe('BrandingSponsorshipModel', () => {
  it('should create an instance', () => {
    expect(new BrandingSponsorshipModel()).toBeTruthy();
  });
});
