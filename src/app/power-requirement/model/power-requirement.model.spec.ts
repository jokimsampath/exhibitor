import { PowerRequirementModel } from "./power-requirement.model";

describe('AdvertisementModel', () => {
  it('should create an instance', () => {
    expect(new PowerRequirementModel()).toBeTruthy();
  });
});
