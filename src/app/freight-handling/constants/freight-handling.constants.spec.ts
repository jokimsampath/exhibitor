import { FreightHandling } from './freight-handling.constants';

describe('FreightHandling', () => {
  it('should create an instance', () => {
    expect(new FreightHandling()).toBeTruthy();
  });
});
