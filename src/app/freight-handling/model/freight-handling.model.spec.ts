import { AirFreightHandlingModel } from "./freight-handling.model";

describe('AirFreightHandlingModel', () => {
  it('should create an instance', () => {
    expect(new AirFreightHandlingModel()).toBeTruthy();
  });
});
